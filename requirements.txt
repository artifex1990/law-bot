# Legal Consultation Bot — зависимости
# pip install -r requirements.txt

# Telegram
aiogram>=3.13.0

# Database
sqlalchemy>=2.0.36
alembic>=1.13.0
asyncpg>=0.29.0
aiosqlite>=0.20.0

# Config & utilities
pydantic>=2.9.0
python-dotenv>=1.0.1

# HTTP API (интеграция со сторонними сервисами)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pyyaml>=6.0.2
aiohttp>=3.11.0
loguru>=0.7.2

# MAX Messenger (опционально)
maxapi>=0.9.0

# S3 backups (опционально, нужен только при BACKUP_STORAGE_TYPE=s3)
# boto3>=1.35.0

# Dev / linting (опционально для разработки)
# ruff>=0.8.0
# mypy>=1.13.0
# pre-commit>=4.0.0

# Type stubs (для mypy/IDE)
types-PyYAML>=6.0.12
