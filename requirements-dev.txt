# Зависимости для разработки (линт, типы, тесты, pre-commit).
# Docker и продакшен используют только requirements.txt — без этого файла.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

ruff>=0.8.0
mypy>=1.13.0
pre-commit>=4.0.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
